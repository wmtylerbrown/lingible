# Core Requirements for All Handlers
# These are the common dependencies needed by all handlers

# AWS Lambda Powertools (core framework)
aws-lambda-powertools>=2.30.0

# AWS SDK
boto3>=1.34.0
botocore>=1.34.0

# Data validation and serialization
pydantic>=2.5.0

# JWT handling (for authorizer)
PyJWT>=2.8.0
cryptography>=45.0.0

# HTTP requests (for receipt validation)
requests>=2.32.5

# Note: Individual handlers may have additional specific dependencies
